query PlayGame($gameId: ID!, $userId: ID!) {
  playGame(gameId: $gameId, userId: ID!){

  }
}

mutation JoinGame($userId: ID!) {
  joinSession(userId: $userId) {
    elapsedTime
  }
}

subscription MatchFound($userId: ID!) {
  matchFound(userId: $userId) {
    id
    createdAt
    lastUpdated
  }
}
