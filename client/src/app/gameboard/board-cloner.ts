import { Square, FileEnum } from './square';
import { Move } from './move';

export default class BoardCloner {
  static cloneBoard(currentBoard: Square[][]): Square[][] {
    const result: Square[][] = [];

    return result;
  }
}
